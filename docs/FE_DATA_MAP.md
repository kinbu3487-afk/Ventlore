# Ventlore · FE Data Map (Bản Đồ Quyết Định Dữ Liệu Tĩnh / Động v1.1)

Tài liệu này xác lập chi tiết từng component và nhóm trường dữ liệu trên toàn bộ giao diện Front-End Ventlore sau đợt cập nhật và sửa lỗi v1.1. Đây là cơ sở cốt lõi để Đội ngũ Kỹ thuật đối chiếu và phát triển Backend (BE), Database, Indexer và Smart Contract sau khi Chủ dự án (Bin) nghiệm thu giao diện.

---

## 1. Phân loại Nguồn Đích (Target Data Sources)

- `FE_STATIC`: Dữ liệu tĩnh, bản dịch i18n, copywriting, brand assets, constants cục bộ.
- `FE_LOCAL_STATE`: Trạng thái tương tác tạm thời của người dùng trên trình duyệt (URL query, form input nháp, tab đang chọn, filter, trạng thái mở modal).
- `BE_DYNAMIC`: Dữ liệu nghiệp vụ động lưu tại cơ sở dữ liệu PostgreSQL và trả về qua REST API (`/api/v1`).
- `WALLET_PUBLIC_READ`: Dữ liệu công khai đọc trực tiếp từ Web3 Provider / RPC (Địa chỉ ví đang kết nối, Chain ID, số dư token ERC-20/ETH, allowance).
- `CHAIN_VIA_BE`: Dữ liệu onchain (Smart Contract events, Splitter receipt, SBT/NFT mint transaction) đã được Indexer/Worker quét, giải mã và trả về qua API tổng hợp có thẩm quyền.

---

## 2. Bảng Ma Trận Dữ Liệu Chi Tiết (16 Cột Chuẩn Hóa)

| Page/route | Actor | Component | Field/data group | Giá trị demo | Nguồn đang dùng | Nguồn đích đề xuất | ID đầu vào | ID tạo/sử dụng lại | Ai xem | Ai sửa | Khi nào tải/làm mới | Trạng thái lỗi/rỗng/chờ | Client state/persist | Gợi ý cache/render | Quyết định cần Bin chốt |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| `/` (Home) | Guest / All | `HomePageView` / Hero | Tiêu đề, slogan, 2 nút CTA | "Hiểu nơi đến. Vững bước đi.", Khám phá điểm đến, Đóng góp khảo sát | `FE_STATIC` | `FE_STATIC` | Không | Không | Công chúng | Admin (CMS) | Khi tải trang / đổi ngôn ngữ | Fallback i18n chữ Việt | None | SSG / Prerender | Copywriting đã chốt 2 CTA duy nhất |
| `/` (Home) | Guest / All | `HomePageView` / Highlights | Danh sách điểm đến tiêu biểu | Cát Cò 3, Móng Rồng, Tây Côn Lĩnh | `api-client` mock | `BE_DYNAMIC` | Không | Tái dùng `placeId` | Công chúng | Admin / System | Khi render Home | Skeleton cards | None | ISR (revalidate 1h) | Tiêu chí chọn điểm nổi bật (tự động hay ghim) |
| `AppShell` (All) | All | `AppShell` / Nav | Header brand, logo ivory, streamlined links | Khám phá, Sứ mệnh, Minh bạch, VIP, Đóng góp | `FE_STATIC` | `FE_STATIC` | Không | Không | Công chúng | Nội bộ FE | Tải layout | Hiển thị navbar rút gọn | Mobile drawer state | Prerender | Đã bỏ Persona Switcher thừa ở Header (dùng ReviewToolbar) |
| `AppShell` (All) | Member+ | `AppShell` / UserMenu | Menu hồ sơ & lối tắt nội bộ | Avatar, Handle, Badge vai trò, Đóng góp, Quyền lợi, Khu kiểm định/Admin, Thoát | SessionContext | `BE_DYNAMIC` | `userId` | Tái dùng `userId` | User đăng nhập | User | Khi session thay đổi | Chế độ Guest (Đăng nhập) | Cookie session | SSR / Client query | Tên hiển thị tự động truncate max 130px tránh tràn hàng |
| `/explore` | All | `SearchFilters` | Bộ lọc vùng, hoạt động, độ khó | Hải Phòng, Quảng Ninh, Hà Giang; Trekking, Kayak | `FE_STATIC` | `BE_DYNAMIC` (Regions) | Không | Tái dùng `regionId` | Công chúng | Admin | Tải trang Explore | Dropdown mặc định | URL SearchParams (`?region=...`) | Client state + query | Danh mục vùng có cần thêm cấp huyện/xã |
| `/explore` | All | `PlaceResults` | Lưới thẻ điểm đến, badges cảnh báo | Ảnh, Tên, Vùng, Số bài, Nhãn an toàn | `api-client` mock | `BE_DYNAMIC` | Search filters | Tái dùng `placeId` | Công chúng | System | Khi thay đổi bộ lọc | "Không tìm thấy địa điểm phù hợp" | URL query | Client cache / SWR | Phân trang bằng Infinite scroll hay Nút bấm |
| `/explore` | All | `PlaceMap` | Bản đồ tương tác Leaflet / Pins | Tọa độ GPS, Markers, Popup card | `api-client` mock | `BE_DYNAMIC` | Bounding box | Tái dùng `placeId` | Công chúng | System | Khi pan/zoom map | Bản đồ xám, giữ pin cũ | Viewport lat/lng | Client Only (no SSR) | Nhà cung cấp bản đồ (OpenStreetMap vs Mapbox) |
| `/places/[placeId]` | All | `PlaceSummary` | Tên điểm, cảnh báo, tọa độ, mô tả | Vịnh Cát Cò 3, Cảnh báo dòng chảy xiết ngầm | `api-client` mock | `BE_DYNAMIC` | `placeId` | Tái dùng `placeId` | Công chúng | Chuyên gia / Admin | Khi vào trang điểm | 404 Place Not Found | None | SSG + ISR | Cảnh báo khẩn cấp có cần nút chia sẻ nhanh |
| `/posts/[postId]` | All | `PostReader` / Dynamic | Bài viết, tác giả, ngày quan sát | Tiêu đề, Tác giả Minh, Nội dung Markdown theo revision | `api-client` mock | `BE_DYNAMIC` | `postId`, `revisionId` | Tái dùng `postId`, `revisionId` | Công chúng | Tác giả gốc | Phản ứng tức thì khi query `revisionId` đổi (Suspense) | 404 Revision Not Found card | URL SearchParams | SSG + Client Suspense | Bọc Suspense ngăn lỗi SSG build, chuyển bản không reload |
| `/posts/[postId]` | All | `VerificationPanel` | Huy hiệu kiểm định, phạm vi, hạn dùng | VERIFIED, UNVERIFIED, NEEDS_CHANGES | `api-client` mock | `BE_DYNAMIC` | `revisionId` | Tái dùng `decisionId` | Công chúng | Ban điều hành | Tự động đồng bộ theo revision đang xem | Thẻ giải thích chi tiết | None | Prerender | Thể hiện trạng thái EXPIRED (Hết hạn) thế nào |
| `/posts/[postId]` | All | `PostReader` / TipCTA | Nút "Tip tác giả", tỷ lệ 80/20 | Nút Tip Author, mở PaymentModal POST_TIP | `api-client` mock | `BE_DYNAMIC` + `CHAIN_VIA_BE` | `postId`, `revisionId` | `routeId` | Công chúng | Tác giả (consent) | Khi render bài | Disabled ("Chưa thể nhận tip") nếu bản chưa VERIFIED | Modal trigger | Client component | Bất biến: Bản chưa duyệt tuyệt đối không nhận tip |
| `PaymentModal` | All | `PaymentModal` / Flow | Khung thanh toán cố định ngữ cảnh | PROJECT (100% quỹ), POST_TIP (80/20), MEMBERSHIP ($15) | `PaymentContext` | `WALLET_PUBLIC_READ` + `BE_DYNAMIC` | Mode, targetId | Sinh `paymentId` / `donationId` | Người thanh toán | Người thanh toán | Mở đúng mode gọi, không cho chuyển tự do | Khóa Guest mua VIP (hiện Gate login) | Context state | Client Modal | Hard invariant: Chặn hoàn toàn Guest mua VIP demo |
| `/vip` | All | `VipPage` / PricingCard | Giá gói, kỳ hạn, danh sách đặc quyền | 15 USD / 12 tháng lịch UTC; Mở khóa báo cáo sâu | `api-client` mock | `BE_DYNAMIC` (Plan config) | `planCode` | Không | Công chúng | Admin cấu hình | Tải trang VIP | Giá mặc định $15 (1500 cents) | None | SSG / ISR | VIP gắn theo userId, gia hạn cộng dồn 365 ngày |
| `/contribute` | Member+ | `ContributeView` / Tab1 | Form viết bài cho điểm có sẵn | Chọn Place, Tiêu đề, Loại bài, Markdown, Claims | `FE_LOCAL_STATE` | `BE_DYNAMIC` | `placeId` | Sinh `postId` + `revisionId` | Tác giả | Tác giả | URL `?tab=existing` 2 chiều | Lỗi phiên bản mới hơn | LocalStorage `draft_${userId}_existing` | Client form | Phân lập nháp riêng biệt theo user và tab |
| `/contribute` | Member+ | `ContributeView` / Tab2 | Form đề xuất điểm mới | Tên điểm, Vùng, Tọa độ GPS, Cảnh báo, Bài khám phá | `FE_LOCAL_STATE` | `BE_DYNAMIC` | `regionId` | Sinh `placeId` + `postId` + `revisionId` | Tác giả | Tác giả | URL `?tab=candidate` 2 chiều | Trùng lặp tiềm năng | LocalStorage `draft_${userId}_candidate` | Client form | Đề xuất tạo đồng thời 3 thực thể, trạng thái Chờ kiểm định |
| `/account` | Member+ | `AccountView` / Profile | Hồ sơ cá nhân & chỉnh sửa demo | Tên, Handle, userId, Giới thiệu ngắn | SessionContext | `BE_DYNAMIC` | `userId` | Tái dùng `userId` | User sở hữu | User | Tải trang cá nhân | Chế độ Guest hiện Gate đăng nhập | Form edit state | SSR / Client query | Bổ sung form chỉnh sửa tên hiển thị và bio lưu demo |
| `/account` | Member+ | `AccountView` / Benefits | Bốn khối quyền lợi sau duyệt | 1. Nhãn web; 2. SBT; 3. NFT; 4. Tip Route 80/20 | `api-client` mock | `CHAIN_VIA_BE` + `BE_DYNAMIC` | `userId` | Tái dùng credentialId / collectibleId | Tác giả | Tác giả | Tải tab quyền lợi | 0 bài duyệt -> Banner cảnh báo + Disable toàn bộ Claim | Query tab | Client query | Không fake OFFERED khi có 0 bài duyệt, bảo đảm trung thực |
| `/expert` | Expert | `ExpertWorkspaceView` / Tasks | Bảng việc thực địa (5 trạng thái) | Offered, In Progress, Submitted, Accepted Work | `api-client` mock | `BE_DYNAMIC` | `expertUserId` | Tái dùng `taskId` | Chuyên gia | Chuyên gia | Tải trang Expert | Guest/Member thấy Gate giới thiệu chuyên gia | Subfilter state | Client query | Permission gate ngăn người dùng thường can thiệp |
| `/admin` | Admin | `AdminWorkspaceView` / Intake | Tiếp nhận hồ sơ & khiếu nại | Danh sách candidate places, bài viết mới, báo sai | `api-client` mock | `BE_DYNAMIC` | Không | Tái dùng intakeId | Ban điều hành | Ban điều hành | Tải trang Admin | Non-admin thấy Gate quyền hạn quản trị | Tab state | Client query | Permission gate bảo vệ khu vực điều hành |
| `/transparency` | All | `PublicLedger` | Sổ cái công khai số dư quỹ & nguồn thu | 45.200 USDC, 3.4 ETH; Quỹ dự án, Phí hội viên | `api-client` mock | `CHAIN_VIA_BE` + `BE_DYNAMIC` | Không | Không | Công chúng | System | Tải trang Minh bạch | Bảng rỗng | Year selector | SSG + ISR (1h) | Tần suất worker đồng bộ số dư onchain |
| Floating | Reviewer | `ReviewToolbar` | Khung điều khiển nghiệm thu 14 kịch bản | 14 kịch bản, 6 personas, Lối tắt nhanh | `FE_LOCAL_STATE` | `FE_LOCAL_STATE` | Không | Không | Bin / QA Reviewer | Reviewer | Tải mọi trang | Thu gọn thành icon pill góc phải dưới | Widget Open state | Client Only | Thanh công cụ chính thống để Bin kiểm thử vai trò |

---

## 3. Danh sách các Quyết định Kỹ thuật Cần Bin Chốt Sau Khi Trải Nghiệm FE v1.1

1. **Lưu trữ bản nháp (Draft persistence):** Hiện tại nháp phân lập lưu tại `localStorage` theo từng tab và user. Sau này có cần đồng bộ nháp lên server (`BE Draft`) khi người dùng đổi thiết bị không?
2. **Quyền truy cập VIP Content:** Mức độ hiển thị nội dung VIP với Guest/Member thường: Làm mờ đoạn văn (blur) hay chỉ hiển thị tóm tắt và khóa toàn bộ phần nhận định chi tiết?
3. **Cảnh báo điểm trùng lặp:** Ngưỡng khoảng cách GPS và độ tương đồng tên gọi: Khi phát hiện khả năng trùng lặp, người dùng có quyền tự quyết định tiếp tục tạo mới hay bắt buộc chuyển qua Admin duyệt?
4. **Chi trả thù lao Chuyên gia:** Việc giải ngân từ Quỹ dự án cho các chuyên gia có công nợ `OPEN` sẽ thực hiện tự động qua Smart Contract định kỳ hàng tuần, hay Admin ký duyệt chi thủ công từng khoản?
5. **Tiền tệ Tip & Quyên góp:** Ventlore hỗ trợ thanh toán tip và quyên góp bằng token ERC-20 (USDC) trên Arbitrum. Có cần mở rộng thêm native token (ETH) hay giải pháp thanh toán thẻ nội địa qua cổng trung gian không?
