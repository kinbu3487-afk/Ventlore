# Ma Trận Bao Phủ 72 Bước Sự Kiện (EVENT_COVERAGE: U01-E01 – U12-E06)

Tài liệu này bao phủ toàn bộ 72 bước sự kiện tương tác giữa người dùng, giao diện, API, cơ sở dữ liệu, smart contract và worker theo `Ventlore_Event_UI_Spec_v0_3.pdf` (Mục 13).

---

## Luồng U01: Khám phá, Đăng nhập và Liên kết Ví (F01; D01, D11)

| Mã Sự Kiện & Actor | Kích hoạt / UI | API / ID Đề xuất | Xử lý và Phản hồi | Chữ ký / Nhánh Lỗi & Ngoại lệ | Trạng thái Chặng 00 |
|---|---|---|---|---|---|
| **U01-E01** (Guest) | S01 / C02, C03. Nhập vùng/hoạt động và tìm kiếm. `UI_QUERY` | `GET /places?regionId=&q=`<br>OUT/ref: `placeId` | Chỉ đọc projection public, phân trang. UI: Danh sách hoặc thông báo chưa có kết quả | Không ký ví; không gas. Ngoại lệ: Không có GPS: chọn vùng thủ công. Lỗi mạng giữ bộ lọc | SPECIFIED |
| **U01-E02** (Guest) | S02 / C04, C06. Chọn PlaceCard. `UI_QUERY` | `GET /places/{placeId}`<br>IN: `placeId`<br>OUT/ref: `placeId`, `revisionId` | Resolve alias về địa điểm canonical, giữ ID nguồn cho lịch sử. UI: Tóm tắt, cảnh báo, bài được phép đọc | Không ký ví; không gas. Ngoại lệ: 404/hidden thông báo phù hợp; không lộ candidate riêng | SPECIFIED |
| **U01-E03** (Reader) | S03 / C05, C06, C07, C08. Chọn bài hoặc bản lịch sử. `UI_QUERY` | `GET /posts/{postId}?revisionId=`<br>IN: `postId`, `revisionId`<br>OUT/ref: `decisionId` | Nạp đúng quyền truy cập và kết luận của revision được chọn. UI: Ngày quan sát, phạm vi, hạn và route đúng bản | Không ký ví; không gas. Ngoại lệ: VIP bị khóa: hiện public summary và CTA xem gói; không tải private | SPECIFIED |
| **U01-E04** (Guest) | S05 / C09, C08. Bấm viết / VIP / donate. `identity.session_started.v1` | `POST /auth/social/callback`<br>OUT/ref: `userId` | Xác minh provider identity; tạo/lấy cùng userId. UI: Trở lại ý định trước và kiểm quyền lại | Session của user/role; không gas. Ngoại lệ: Hủy/timeout giữ ý định; provider mismatch không gộp account | SPECIFIED |
| **U01-E05** (Member) | S04 / C01, C49. Login thành công hoặc mở app. `UI_QUERY` | `GET /me`<br>IN: `userId`<br>OUT/ref: `roleAssignmentId`, `membershipId` | Đọc quyền, scope, kỳ VIP; không cấp quyền từ token balance. UI: Điều hướng đúng vai trò; vẫn cùng userId | Session của user/role; không gas. Ngoại lệ: 401 đăng nhập lại; role hết hạn ẩn tác vụ có lý do | SPECIFIED |
| **U01-E06** (Member) | S20 / C10, C46. Chủ tài khoản chọn kết nối và ký. `wallet.binding_verified.v1` | `POST /wallet-bindings/challenge`<br>`POST /wallet-bindings`<br>IN: `userId`<br>OUT/ref: `walletBindingId` | Verify nonce/domain/expiry/address; lưu binding sau proof. UI: Ví đã xác minh; quay về tác vụ cần ví | Chủ ví ký thông điệp liên kết; không gas onchain. Ngoại lệ: Sai account/hết nonce/từ chối: xin challenge mới | SPECIFIED |

---

## Luồng U02: Đề Xuất Địa Điểm Mới (F02; D02)

| Mã Sự Kiện & Actor | Kích hoạt / UI | API / ID Đề xuất | Xử lý và Phản hồi | Chữ ký / Nhánh Lỗi & Ngoại lệ | Trạng thái Chặng 00 |
|---|---|---|---|---|---|
| **U02-E01** (Member) | S17 / C11, C08. Chọn đóng góp điểm đến mới. `UI_QUERY` | `GET /contribution-form-config`<br>IN: `userId`<br>OUT/ref: Giữ hồ sơ hiện tại | Khởi tạo nháp tạm; kiểm quyền đăng và cấu hình trường. UI: Tên, vị trí, hoạt động, ngày đến, rủi ro | Session của user/role; không gas. Ngoại lệ: Chưa login: U01 rồi quay lại. Không tự tạo placeId từ tọa độ | SPECIFIED |
| **U02-E02** (Member) | S18 / C14, C04. Nhập vị trí/tên và kiểm tra. `UI_QUERY` | `POST /places/duplicate-check`<br>OUT/ref: `placeId` | Trả candidates được phép xem; chưa merge hay tạo place. UI: Chọn điểm có sẵn -> U03; không rõ -> ghi chú Operator | Session của user/role; không gas. Ngoại lệ: Lỗi mạng giữ nháp; không biến lỗi tra cứu thành kết luận không trùng | SPECIFIED |
| **U02-E03** (Author) | S17 / C11, C12, C13. Nhập trải nghiệm, tải ảnh. `media.ready.v1` | `POST /media`<br>`GET /media/{mediaId}`<br>OUT/ref: `mediaId` | Upload có quyền, xử lý/kiểm file; evidence riêng ở hồ sơ gửi. UI: Từng file READY; claims dự thảo rõ phạm vi | Session của user/role; không gas. Ngoại lệ: File lỗi giữ phần còn lại; chặn gửi khi file bắt buộc chưa ready | SPECIFIED |
| **U02-E04** (Author) | S07 / C15, C08. Bấm xem trước. `UI_QUERY` | `POST /contributions/validate`<br>OUT/ref: Giữ hồ sơ hiện tại | Validation không cấp quyết định; hiện quyền dùng nội dung và REVIEW_ONLY. UI: Xác nhận nội dung, nguồn, quyền chia sẻ | Session của user/role; không gas. Ngoại lệ: Thiếu ngày/nguồn/consent: focus trường lỗi, quay về sửa | SPECIFIED |
| **U02-E05** (Author) | S17 / C15, C46. Bấm gửi đề xuất với Idempotency-Key. `place.proposal_submitted.v1` | `POST /place-proposals`<br>IN: `userId`, `mediaId`<br>OUT/ref: `placeId`, `postId`, `revisionId`, `claimId` | Trong transaction: kiểm trùng; tạo candidate + DISCOVERY + snapshot. Trùng do race -> 409 yêu cầu chọn lại. UI: Mã bài và REVIEW_ONLY/UNVERIFIED | Session của user/role; không gas. Ngoại lệ: Timeout tra lại cùng key; không tạo lại các ID. 409 giữ nháp và mở S18 | SPECIFIED |
| **U02-E06** (Author / Operator) | S08 / C16, C17, C47. Server hoàn tất lọc cơ bản. `contribution.screened.v1` | `GET /posts/{postId}/timeline`<br>IN: `postId`, `revisionId`<br>OUT/ref: Giữ hồ sơ hiện tại | Ghi kết quả sàng lọc; Operator chọn tiếp U04, không tự mở reviewCase. UI: Chờ chọn kiểm tra hoặc phản hồi thiếu thông tin | Session của user/role; không gas. Ngoại lệ: Spam/trùng/cần sửa có lý do; candidate chưa được bán như VIP | SPECIFIED |

---

## Luồng U03: Viết Bài và Cập Nhật Phiên Bản (F03-F04; D03)

| Mã Sự Kiện & Actor | Kích hoạt / UI | API / ID Đề xuất | Xử lý và Phản hồi | Chữ ký / Nhánh Lỗi & Ngoại lệ | Trạng thái Chặng 00 |
|---|---|---|---|---|---|
| **U03-E01** (Author) | S06 / C11, C04. Chọn Tôi đã đến đây. `post.draft_created.v1` | `POST /posts`<br>IN: `placeId`<br>OUT/ref: `postId` | API kiểm place dùng được, tạo post theo userId session. UI: Nháp bài thuộc đúng tác giả và place | Session của user/role; không gas. Ngoại lệ: Không truyền authorUserId tùy ý; place merged trả canonical | SPECIFIED |
| **U03-E02** (Author) | S06 / C11, C12, C13. Nhập và lưu nháp. `post.draft_saved.v1` | `PATCH /posts/{postId}/draft`<br>IN: `postId`, `mediaId`<br>OUT/ref: Giữ hồ sơ hiện tại | Chỉ sửa workspace nháp, chưa thay snapshot đã gửi. Dùng expectedVersion. UI: Saved; có thể rời rồi mở lại | Session của user/role; không gas. Ngoại lệ: 409: chọn tải bản mới/so sánh; không ghi đè song song | SPECIFIED |
| **U03-E03** (Author) | S07 / C15, C06. Bấm xem trước. `UI_QUERY` | `POST /posts/{postId}/validate`<br>IN: `postId`<br>OUT/ref: Giữ hồ sơ hiện tại | Validation và preview, nhãn Chưa kiểm tra. UI: Cho sửa hoặc gửi | Session của user/role; không gas. Ngoại lệ: 422 hiện từng trường; không mất nội dung | SPECIFIED |
| **U03-E04** (Author) | S07 / C15, C46. Bấm đăng với Idempotency-Key. `post.revision_submitted.v1` | `POST /posts/{postId}/revisions`<br>IN: `postId`, `mediaId`<br>OUT/ref: `revisionId`, `claimId` | Freeze snapshot; sàng lọc riêng trước PUBLISHED + UNVERIFIED. UI: Mã revision và trạng thái đang lọc / đã đăng | Session của user/role; không gas. Ngoại lệ: Timeout truy cùng key; failed moderation giữ riêng có lý do | SPECIFIED |
| **U03-E05** (Author) | S19 / C16, C17, C07. Mở thông báo hoặc tiến trình. `UI_QUERY` | `GET /posts/{postId}/timeline`<br>IN: `postId`, `revisionId`<br>OUT/ref: `reviewCaseId`, `decisionId` | Chỉ đọc kết quả và phần bằng chứng được phép. UI: Hiện phiên bản nguồn của yêu cầu sửa | Session của user/role; không gas. Ngoại lệ: Thông báo cũ: tải state mới; không tự nâng nhãn bản hiện tại | SPECIFIED |
| **U03-E06** (Author) | S06 / C11, C17, C15. Tác giả sửa và gửi lại. `post.revision_replaced.v1` | `POST /posts/{postId}/revisions`<br>IN: `postId`, `revisionId`<br>OUT/ref: `revisionId`, `claimId` | Input revisionId là parent; output revisionId mới. Operator mở case mới khi chọn. UI: Post giữ nguyên; bản mới UNVERIFIED | Session của user/role; không gas. Ngoại lệ: Đang review không tráo snapshot task; xử lý công task cũ riêng | SPECIFIED |

---

## Luồng U04: Ưu Tiên, Mở Hồ Sơ và Giao Việc (F04; D03-D04)

| Mã Sự Kiện & Actor | Kích hoạt / UI | API / ID Đề xuất | Xử lý và Phản hồi | Chữ ký / Nhánh Lỗi & Ngoại lệ | Trạng thái Chặng 00 |
|---|---|---|---|---|---|
| **U04-E01** (Operator) | S09 / C18, C06. Operator lọc và mở bài. `UI_QUERY` | `GET /admin/submissions`<br>IN: `revisionId`<br>OUT/ref: Giữ hồ sơ hiện tại | Xếp theo KPI factors và policyVersion; ghi lý do khi ưu tiên thủ công. UI: Thấy độ mới, ảnh hưởng, bằng chứng | Session của user/role; không gas. Ngoại lệ: Không xem bài ngoài scope; dữ liệu stale phải refresh | SPECIFIED |
| **U04-E02** (Operator) | S09 / C18, C14. Lưu kết quả sàng lọc. `review.screening_recorded.v1` | `POST /admin/revisions/{id}/screening`<br>IN: `revisionId`, `placeId`<br>OUT/ref: Giữ hồ sơ hiện tại | Moderation/gộp có audit và kiểm canonical không chu trình. UI: Bài đủ đầu vào hoặc cần sửa; hiện loại task đúng | Session của user/role; không gas. Ngoại lệ: Đã giao task mới phát hiện trùng: xử lý công cũ trước giao mới | SPECIFIED |
| **U04-E03** (Operator) | S12 / C19. Xác nhận revision và claim cần kiểm. `review.case_opened.v1` | `POST /review-cases`<br>IN: `revisionId`, `claimId`<br>OUT/ref: `reviewCaseId` | Khóa revision + scope, lưu mức kiểm tra và policyVersion. UI: Case mở; loại NEW_PLACE hoặc EXISTING_PLACE | Session của user/role; không gas. Ngoại lệ: Không trỏ claim ngoài revision; case cũ kết thúc tạo case mới có previousCaseId | SPECIFIED |
| **U04-E04** (Operator) | S26 / C20, C19. Chọn assignee và phạm vi. `UI_QUERY` | `POST /review-cases/{id}/assignment-check`<br>IN: `reviewCaseId`, `userId`<br>OUT/ref: Giữ hồ sơ hiện tại | Kiểm chuyên môn, vùng, hạn role và xung đột; P04 cần thêm người khi rủi ro cao. UI: Eligible hoặc WAITING_CAPACITY | Session của user/role; không gas. Ngoại lệ: Tác giả/cùng người giả 2 account: từ chối giao; Operator xung đột cần thay | SPECIFIED |
| **U04-E05** (Operator) | S26 / C21, C20. Chốt công, tài sản, deadline và gửi. `task.offered.v1` | `POST /review-cases/{id}/task-offers`<br>IN: `reviewCaseId`, `userId`<br>OUT/ref: `taskId`, `reservationId` | Khóa ngân sách theo asset, tạo reserve + task OFFERED + outbox trong transaction. UI: Offer rõ tiền, hạn, hủy | Session của user/role; không gas. Ngoại lệ: Insufficient/race: rollback; chưa có task đã cam kết. Retry cùng key | SPECIFIED |
| **U04-E06** (Expert) | S25 / C22, C47. Outbox đã giao và chuyên gia mở việc. `UI_QUERY` | `GET /work/tasks`<br>IN: `taskId`, `userId`<br>OUT/ref: Giữ hồ sơ hiện tại | Notification chỉ dẫn tới nguồn; tải lại task và kiểm assignee. UI: OFFERED với Nhận / Từ chối; tiếp U05 | Session của user/role; không gas. Ngoại lệ: Push lỗi không mất task; list work là nguồn truy xuất | SPECIFIED |

---

## Luồng U05: Chuyên Gia Nhận Việc và Nộp Kết Quả (F05; D04)

| Mã Sự Kiện & Actor | Kích hoạt / UI | API / ID Đề xuất | Xử lý và Phản hồi | Chữ ký / Nhánh Lỗi & Ngoại lệ | Trạng thái Chặng 00 |
|---|---|---|---|---|---|
| **U05-E01** (Operator + Invited) | S24 / C48, C49. Chọn người đóng góp; phản hồi lời mời. `expert.role_response_recorded.v1` | `POST /role-assignments`<br>`POST /role-assignments/{id}/response`<br>IN: `userId`, `regionId`<br>OUT/ref: `roleAssignmentId` | Đánh giá năng lực/địa bàn, tạo invite có scope/hạn. Chỉ active sau chấp nhận. UI: Nhận lời mời; chấp nhận mở Việc của tôi | Session của user/role; không gas. Ngoại lệ: Sai user/hết hạn/từ chối: không active; không dùng SBT để cấp quyền | SPECIFIED |
| **U05-E02** (Assigned Expert) | S10 / C22, C23. Chọn task ở /work. `UI_QUERY` | `GET /work/tasks/{taskId}`<br>IN: `taskId`<br>OUT/ref: `revisionId`, `reviewCaseId` | Đọc snapshot, điều khoản và phạm vi quyền. UI: Hiện tiền công, hạn nộp, vùng và xung đột | Session của user/role; không gas. Ngoại lệ: 403 khi không phải assignee; không lộ bằng chứng của expert khác | SPECIFIED |
| **U05-E03** (Assigned Expert) | S10 / C23, C20. Bấm nhận hoặc từ chối có lý do. `task.response_recorded.v1` | `POST /tasks/{taskId}/response`<br>IN: `taskId`<br>OUT/ref: Giữ hồ sơ hiện tại | OFFERED -> ACCEPTED / DECLINED; server kiểm deadline và role. UI: Nhận mở làm việc; từ chối trả hàng đợi | Session của user/role; không gas. Ngoại lệ: Offer expired/409: tải trạng thái hiện tại; không đổi assignee task cũ | SPECIFIED |
| **U05-E04** (Assigned Expert) | S10 / C22, C24. Bấm bắt đầu sau khi nhận. `task.work_started.v1` | `POST /tasks/{taskId}/start`<br>IN: `taskId`<br>OUT/ref: Giữ hồ sơ hiện tại | ACCEPTED -> IN_PROGRESS, có audit. UI: Checklist theo scope và revision đã khóa | Session của user/role; không gas. Ngoại lệ: Task cancelled/role bị thu hồi: chặn thao tác, xử lý công đã làm | SPECIFIED |
| **U05-E05** (Assigned Expert) | S11 / C24, C12, C13. Nhập quan sát và bằng chứng. `task.evidence_draft_saved.v1` | `PATCH /tasks/{id}/submission-draft`<br>`POST /media`<br>IN: `taskId`, `mediaId`<br>OUT/ref: Giữ hồ sơ hiện tại | Nháp riêng; không thay revision bài. GPS/ảnh có quyền công khai riêng. UI: Đủ trường bắt buộc, có kết luận và điều chưa rõ | Session của user/role; không gas. Ngoại lệ: Offline giữ nháp có cảnh báo; upload chưa READY thì chưa nộp | SPECIFIED |
| **U05-E06** (Assigned Expert) | S11 / C25, C24. Xác nhận nộp báo cáo. `task.submission_received.v1` | `POST /tasks/{taskId}/submissions`<br>IN: `taskId`, `mediaId`<br>OUT/ref: `submissionId`, `evidenceId` | Freeze bản nộp, task -> SUBMITTED; mỗi lần bổ sung append-only. UI: Mã bản nộp và chờ nghiệm thu U06 | Session của user/role; không gas. Ngoại lệ: Retry cùng key không nhân bản; NEEDS_MORE quay bước ghi nhận | SPECIFIED |

---

## Luồng U06: Nghiệm Thu và Quyết Định Nội Dung (F04-F06; D04-D05)

| Mã Sự Kiện & Actor | Kích hoạt / UI | API / ID Đề xuất | Xử lý và Phản hồi | Chữ ký / Nhánh Lỗi & Ngoại lệ | Trạng thái Chặng 00 |
|---|---|---|---|---|---|
| **U06-E01** (Operator) | S12 / C25, C19. Operator mở case có kết quả. `UI_QUERY` | `GET /review-cases/{reviewCaseId}`<br>IN: `reviewCaseId`<br>OUT/ref: `revisionId`, `taskId`, `submissionId` | Đọc chứng cứ theo quyền, phạm vi và mức độc lập yêu cầu. UI: So sánh kết quả; thấy dữ liệu chưa đủ | Session của user/role; không gas. Ngoại lệ: Thiếu người theo P04: giữ chờ, không giảm chuẩn tự động | SPECIFIED |
| **U06-E02** (Acceptance Op) | S27 / C26. Chọn chấp nhận công / bổ sung / từ chối. `task.acceptance_decided.v1` | `POST /tasks/{id}/acceptance-decision`<br>IN: `taskId`, `submissionId`<br>OUT/ref: `acceptanceId`, `payableId` | Chỉ ACCEPTED_WORK tạo acceptance + payable một lần, đúng payee/asset/fee. UI: Công đạt -> OPEN payable để U07 | Session của user/role; không gas. Ngoại lệ: Bài bị REJECTED vẫn có thể công đạt; duplicate trả cùng kết quả | SPECIFIED |
| **U06-E03** (Independent Op)| S12 / C27. Chọn 1 trong 4 kết quả. `review.decision_recorded.v1` | `POST /review-cases/{id}/decision`<br>IN: `reviewCaseId`, `revisionId`<br>OUT/ref: `decisionId` | Quyết định kết thúc, bất biến; APPROVED có phạm vi và validUntil. UI: APPROVED/CHANGES_REQUESTED/INCONCLUSIVE/REJECTED | Session của user/role; không gas. Ngoại lệ: Xung đột/sai revision/đã có decision: từ chối. Không duyệt vì công đạt | SPECIFIED |
| **U06-E04** (Author) | S19 / C17, C16. Quyết định cần sửa hoặc chưa đạt. `UI_QUERY` | `GET /posts/{postId}/timeline`<br>IN: `postId`, `revisionId`, `decisionId`<br>OUT/ref: Giữ hồ sơ hiện tại | Hiện lý do và phạm vi được phép; giữ lịch sử, không lộ private reviewer. UI: Sửa theo U03 hoặc bổ sung; không cấp quyền APPROVED | Session của user/role; không gas. Ngoại lệ: Không tự coi INCONCLUSIVE là tin sai; không sửa decision cũ | SPECIFIED |
| **U06-E05** (Operator / Service)| S35 / C06, C39. APPROVED đã được lưu. `review.publication_applied.v1` | `POST /decisions/{id}/publication`<br>IN: `decisionId`, `revisionId`, `placeId`<br>OUT/ref: Giữ hồ sơ hiện tại | Project nhãn đúng revision; điểm mới có thể ACTIVE. VIP cần consent theo P03. UI: Nhãn có ngày, hạn, nguồn | Session của user/role; không gas. Ngoại lệ: Thiếu consent không đưa vào VIP; không tự khóa bài public; hold chặn quảng bá | SPECIFIED |
| **U06-E06** (Service / Operator)| S16 / C28. Quyết định APPROVED hợp lệ. `contribution.benefits_evaluated.v1` | `POST /decisions/{id}/benefit-evaluation`<br>IN: `decisionId`, `postId`, `userId`<br>OUT/ref: `credentialId`, `collectibleId`, `routeId` | Chỉ tạo bản ghi đủ điều kiện còn thiếu; unique theo baseline; không mint tự động. UI: Bốn thẻ độc lập -> U08 | Session của user/role; không gas. Ngoại lệ: Bản ghi đã có thì trả lại ID; chưa có ví vẫn giữ công nhận | SPECIFIED |

---

## Luồng U07: Quỹ Trả Công Chuyên Gia (F05, F10; D04, D09)

| Mã Sự Kiện & Actor | Kích hoạt / UI | API / ID Đề xuất | Xử lý và Phản hồi | Chữ ký / Nhánh Lỗi & Ngoại lệ | Trạng thái Chặng 00 |
|---|---|---|---|---|---|
| **U07-E01** (Treasury) | S28 / C42, C43. Quản lý quỹ chọn khoản OPEN. `UI_QUERY` | `GET /payables?status=OPEN`<br>OUT/ref: `payableId` | Đọc riêng từng tài sản và tiền đã cam kết. UI: Danh sách người nhận, task, số tiền và nguồn | Session của user/role; không gas. Ngoại lệ: Không cộng token khác nhau; Operator nội dung không có quyền chi | SPECIFIED |
| **U07-E02** (Treasury) | S29 / C43, C45. Mở payable và xác nhận thông tin. `UI_QUERY` | `GET /payables/{payableId}`<br>IN: `payableId`<br>OUT/ref: `acceptanceId`, `taskId` | Lần ngược PK/FK; người nhận/tài sản/mức công phải khớp task. UI: Chỉ đọc điều khoản; không có ô sửa số tiền | Session của user/role; không gas. Ngoại lệ: Phát hiện sai: dừng, đối soát; không tạo payable khác cho cùng acceptance | SPECIFIED |
| **U07-E03** (Treasury) | S29 / C43, C34. Bấm chuẩn bị thanh toán. `payout.prepared.v1` | `POST /payables/{id}/payouts`<br>IN: `payableId`<br>OUT/ref: `payoutId`, `actionId` | Giữ tối đa 1 lệnh pending/unknown; chuẩn bị calldata payTask đúng key/deployment. UI: Xác nhận ví quỹ, payee, asset, gas | Session của user/role; không gas. Ngoại lệ: Đã PAID trả lịch sử; pending/unknown chuyển U12; thiếu ngân sách không ký | SPECIFIED |
| **U07-E04** (W_TREASURY) | S29 / C34. Allowance chưa đủ. `CHAIN_ACTION` | `POST /payouts/{id}/approval-actions`<br>`token.approve`<br>IN: `payoutId`<br>OUT/ref: `actionId`, `attemptId` | Tạo actionId riêng cho approve, khác action payTask. Ví quỹ ký đúng spender; đọc lại allowance. UI: Đủ quyền mới mở bước trả | Ví quỹ ký và trả gas; chuyên gia không ký. Ngoại lệ: Từ chối/revert approve: payable chưa PAID; thiếu gas hiển thị rõ | SPECIFIED |
| **U07-E05** (W_TREASURY) | S29 / C34, C43. Ví quỹ xác nhận payTask. `CHAIN_ACTION` | `payTask`<br>`POST /actions/{actionId}/attempts`<br>IN: `payableId`, `actionId`<br>OUT/ref: `attemptId` | Dùng action payTask từ E03. Broadcast ghi attempt; payable PAYMENT_PENDING. Safe kiểm thao tác bên trong. UI: Hiện đã gửi | Ví quỹ ký và trả gas; ví chuyên gia nhận không ký. Ngoại lệ: Account/chain mismatch chặn; timeout -> UNKNOWN | SPECIFIED |
| **U07-E06** (Worker) | S29 / C35, C44, C47. Indexer đối soát thành công và finality. `payout.finalized.v1` | `GET /payables/{payableId}`<br>IN: `payableId`, `actionId`<br>OUT/ref: Giữ hồ sơ hiện tại | Đúng event/payload + finality -> ghi PAID và ledger một lần. UI: Chuyên gia thấy Đã trả công, số tiền nhận và tx | Không ký ví; không gas. Ngoại lệ: Reorg trước finalized gỡ projection sớm; không xác minh finality thì chờ | SPECIFIED |

---

## Luồng U08: SBT, NFT Tác Giả và Quyền Nhận Tip (F06, F10; D05, D09)

| Mã Sự Kiện & Actor | Kích hoạt / UI | API / ID Đề xuất | Xử lý và Phản hồi | Chữ ký / Nhánh Lỗi & Ngoại lệ | Trạng thái Chặng 00 |
|---|---|---|---|---|---|
| **U08-E01** (Author) | S16 / C28, C06. Tác giả mở bài được duyệt. `UI_QUERY` | `GET /posts/{postId}/benefits`<br>IN: `postId`, `revisionId`<br>OUT/ref: `credentialId`, `collectibleId`, `routeId` | Đọc từng nhánh, lý do chờ và token/route đã có. UI: Nhãn có thể VERIFIED khi token chưa cấp và route chưa ACTIVE | Session của user/role; không gas. Ngoại lệ: P05/P06 là baseline; không mint thêm cho bài đã có NFT | SPECIFIED |
| **U08-E02** (W_AUTHOR) | S16 / C10, C30. Tác giả chọn ví và đồng ý. `tip.consent_recorded.v1` | `POST /routes/{id}/consent-challenge`<br>`POST /routes/{id}/consent`<br>IN: `routeId`, `walletBindingId`<br>OUT/ref: Giữ hồ sơ | Verify consent đúng beneficiary, revision, deployment, hạn, payload. UI: AWAITING_CONSENT -> sẵn sàng đăng ký | Ví tác giả ký consent; không gas onchain. Ngoại lệ: Đổi ví/hạn/payload xin consent mới; không tạo revision giả | SPECIFIED |
| **U08-E03** (W_OPS) | S32 / C31, C34. Operator kiểm gate và ký đăng ký. `tip.route_activated.v1` | `POST /routes/{id}/registration-action`<br>`registerRoute`<br>IN: `routeId`, `decisionId`<br>OUT/ref: `actionId`, `attemptId` | W_OPS ký; worker đối soát -> ACTIVE theo chính sách. Trước đó PENDING. UI: Mở CTA khi route hiện hành, còn hạn, không hold | Ví Operator có đúng role contract ký và trả gas. Ngoại lệ: Tx lỗi vẫn có nhãn; event ACTIVE cũ không xóa hold | SPECIFIED |
| **U08-E04** (W_OPS) | S32 / C29, C34. Operator xét SBT hoặc NFT còn thiếu. `grant.authorized.v1` | `POST /credentials/{id}/authorization-action`<br>hoặc `POST /collectibles/{id}/...`<br>IN: `walletBindingId`<br>OUT/ref: `actionId`, `attemptId` | Hai endpoint/action độc lập; mỗi lần dùng đúng một ID. Chỉ authorize sau khi recipient được xác minh. UI: AUTHORIZED cho đúng subject | Ví Operator có đúng role contract ký và trả gas. Ngoại lệ: Sai ví/đã cấp/hết điều kiện: chặn; chưa có ví giữ ELIGIBLE | SPECIFIED |
| **U08-E05** (W_AUTHOR) | S16 / C29, C34. Chủ ví đọc điều kiện và claim. `CHAIN_ACTION` | `claim-action`<br>`wallet call`<br>IN: `credentialId` XOR `collectibleId`<br>OUT/ref: `actionId`, `attemptId` | Dùng đúng 1 ID mỗi claim; giữ business key khi retry. UI: PENDING; hiện phí mạng và ví nhận | Ví tác giả ký giao dịch và trả gas. Ngoại lệ: Từ chối ký không mất eligibility; wrong subject/expired/claimed bị chặn | SPECIFIED |
| **U08-E06** (Worker) | S16 / C29, C28. Worker xác minh mint theo deployment. `grant.issued.v1` | `GET /posts/{postId}/benefits`<br>IN: `credentialId`, `collectibleId`<br>OUT/ref: Giữ hồ sơ hiện tại | ISSUED sau đối soát; token identity chain + contract + tokenId. UI: NFT ghi original author/revision; SBT không tự cấp role | Không ký ví; không gas. Ngoại lệ: Transfer/burn không mint lại; lịch sử còn và tip không đổi theo ownerOf | SPECIFIED |

---

## Luồng U09: Donate Dự Án hoặc Bài Viết (F07, F10; D06)

| Mã Sự Kiện & Actor | Kích hoạt / UI | API / ID Đề xuất | Xử lý và Phản hồi | Chữ ký / Nhánh Lỗi & Ngoại lệ | Trạng thái Chặng 00 |
|---|---|---|---|---|---|
| **U09-E01** (Member) | S13 / C32, C08. Chọn dự án hoặc bài đủ điều kiện. `UI_QUERY` | `GET /donation-config`<br>`GET /revisions/{id}/tip-eligibility`<br>IN: `revisionId`<br>OUT/ref: `routeId` | PROJECT không yêu cầu route; POST_TIP kiểm bản và gate. UI: Số tiền, token và người nhận rõ trước bước ví | Session của user/role; không gas. Ngoại lệ: Bản chưa duyệt/hold/hết hạn: khóa tip và nêu lý do; vẫn chọn được PROJECT | SPECIFIED |
| **U09-E02** (Donor) | S14 / C33, C10. Xác minh ví, nhập tiền và tiếp tục. `donation.prepared.v1` | `POST /donations`<br>IN: `routeId`, `walletBindingId`<br>OUT/ref: `donationId`, `actionId` | Lưu kind, donor, amount, route nếu có, requestKey; server tính số nguyên. UI: Preview 100% hoặc 80/20, gas riêng | Session của user/role; không gas. Ngoại lệ: Thiếu ví dùng U01; quote cũ làm mới sau đối soát; đổi ý định xử lý an toàn | SPECIFIED |
| **U09-E03** (W_DONOR) | S14 / C34. Allowance chưa đủ. `CHAIN_ACTION` | `POST /donations/{id}/approval-actions`<br>`token.approve`<br>IN: `donationId`<br>OUT/ref: `actionId`, `attemptId` | Tạo actionId riêng cho approve, khác action donate. Ký đúng token/spender/amount; đọc lại allowance. UI: Đủ quyền chi | Ví donor ký giao dịch và trả gas. Ngoại lệ: Từ chối/revert chưa chuyển tiền; không hiện biên nhận donate | SPECIFIED |
| **U09-E04** (W_DONOR) | S14 / C34, C33. Donor xác nhận sau simulation. `CHAIN_ACTION` | `donateProject` OR `tipContribution`<br>`POST /actions/{id}/attempts`<br>IN: `donationId`, `actionId`<br>OUT/ref: `attemptId` | Dùng action donate từ E02, không action approve. Kiểm account/chain/route/deadline; broadcast ghi attempt. UI: SUBMITTED | Ví donor ký giao dịch và trả gas. Ngoại lệ: Revert/từ chối/unknown hiện khác nhau; không mặc định tạo DON mới | SPECIFIED |
| **U09-E05** (Worker) | S15 / C34, C44. Thấy thực thi hợp lệ. `donation.observed.v1` | `GET /donations/{donationId}/status`<br>IN: `donationId`, `actionId`<br>OUT/ref: Giữ hồ sơ hiện tại | Đối chiếu receipt/event/contract/payload/block; chỉ OBSERVED trước finality. UI: Đã thấy giao dịch; đang chờ xác nhận | Không ký ví; không gas. Ngoại lệ: Reorg gỡ observed; mất callback quét logs; indexer lag vẫn giữ pending | SPECIFIED |
| **U09-E06** (Worker) | S15 / C35, C47. Đạt finality theo deployment. `donation.finalized.v1` | `GET /donations/{donationId}`<br>IN: `donationId`<br>OUT/ref: Giữ hồ sơ hiện tại | Ghi DonationRecorded/receipt 1 lần; ledger không cộng thêm từ Transfer. UI: FINALIZED: đúng split; không cấp VIP/NFT donor | Không ký ví; không gas. Ngoại lệ: Direct-chain receipt lưu donor address, có thể chưa có userId; đối soát rồi mới gắn | SPECIFIED |

---

## Luồng U10: Mua VIP và Gia Hạn (F08; D07)

| Mã Sự Kiện & Actor | Kích hoạt / UI | API / ID Đề xuất | Xử lý và Phản hồi | Chữ ký / Nhánh Lỗi & Ngoại lệ | Trạng thái Chặng 00 |
|---|---|---|---|---|---|
| **U10-E01** (Reader) | S03 / C08, C06. Reader mở phần hạn chế. `UI_QUERY` | `GET /posts/{postId}?revisionId=`<br>IN: `postId`, `revisionId`<br>OUT/ref: `membershipId` | API kiểm entitlement khi có session; guest chỉ nhận public summary. UI: Xem quyền lợi/gói, cảnh báo vẫn đọc được | Không ký ví; không gas. Ngoại lệ: Không dùng CSS để giấu nội dung đã tải; không quảng bá phần đang hold | SPECIFIED |
| **U10-E02** (Member) | S21 / C36, C09. Chọn mua/gia hạn sau login. `UI_QUERY` | `GET /vip/plans`<br>OUT/ref: `membershipId` | Giá niêm yết 1500 USD cents; 12 tháng lịch; chưa tạo nghĩa vụ tiền. UI: Quyền lợi, thời hạn và kênh có sẵn | Session của user/role; không gas. Ngoại lệ: Chưa cấu hình kênh: chưa nhận thanh toán, giữ giới thiệu gói | SPECIFIED |
| **U10-E03** (Member) | S22 / C37. Xác nhận gói và phương thức. `vip.payment_created.v1` | `POST /vip/payments`<br>IN: `membershipId`<br>OUT/ref: `membershipId`, `paymentId` | Tạo/lấy membership theo user+plan; payment mới cho lần mua thật, quote immutable. UI: Đơn chờ thanh toán với tiền, hạn | Session của user/role; không gas. Ngoại lệ: Retry cùng key trả cùng payment; quote hết hạn không tự đổi tiền đã chốt | SPECIFIED |
| **U10-E04** (Payer) | S22 / C37, C34. Tiếp tục tới kênh đã chọn. `EXTERNAL_ACTION` | `POST /vip/payments/{id}/checkout`<br>IN: `paymentId`<br>OUT/ref: `actionId`, `attemptId` | Fiat dùng provider flow; crypto tạo action và chủ ví ký. UI: Hiện pending, cho mở lại đơn sau redirect | Fiat theo provider; crypto do ví người mua ký và trả gas. Ngoại lệ: Return URL không đủ xác nhận; không auto-debit năm sau | SPECIFIED |
| **U10-E05** (Worker) | S23 / C38, C47. Callback hợp lệ hoặc finalized. `vip.term_granted.v1` | `GET /vip/payments/{id}`<br>`GET /me/membership`<br>IN: `paymentId`<br>OUT/ref: Giữ hồ sơ | CONFIRMED và cấp term 1 lần; khóa membership để nối kỳ: startsAt=max(confirmedAt,endsAt), +12 tháng. UI: ACTIVE | Không ký ví; không gas. Ngoại lệ: Callback lặp không thêm kỳ; failed/unknown không cấp VIP. Ngày 29/2 dùng cuối tháng | SPECIFIED |
| **U10-E06** (Clock / Member)| S23 / C38, C36. Đến endsAt hoặc user chọn gia hạn. `vip.entitlement_expired.v1` | `GET /me/membership`<br>`POST /vip/payments`<br>IN: `membershipId`<br>OUT/ref: `paymentId` | Clock làm entitlement EXPIRED; chỉ user mua tiếp mới tạo payment mới. UI: Mất quyền VIP khi hết hạn; giữ bài/role/SBT/NFT | Đọc/tính quyền theo server clock; mua tiếp là tác vụ mới có session. Ngoại lệ: Renew pending hiển thị riêng kỳ hiện tại | SPECIFIED |

---

## Luồng U11: Báo Sai, Hold và Kiểm Tra Lại (F09-F10; D10)

| Mã Sự Kiện & Actor | Kích hoạt / UI | API / ID Đề xuất | Xử lý và Phản hồi | Chữ ký / Nhánh Lỗi & Ngoại lệ | Trạng thái Chặng 00 |
|---|---|---|---|---|---|
| **U11-E01** (Member) | S30 / C40, C12. Chọn nhận định/bản có vấn đề. `report.received.v1` | `POST /reports`<br>IN: `revisionId`, `claimId`<br>OUT/ref: `reportId` | Ghi report, reason và chứng cứ riêng, outbox trong transaction. UI: Mã theo dõi; bài chưa tự chuyển REJECTED | Session của user/role; không gas. Ngoại lệ: Rate limit/thiếu nguồn: lỗi có hướng dẫn; không lộ danh tính người báo | SPECIFIED |
| **U11-E02** (Operator) | S31 / C41, C19. Operator xem report trong scope. `report.triaged.v1` | `POST /reports/{id}/triage`<br>IN: `reportId`, `revisionId`<br>OUT/ref: Giữ hồ sơ hiện tại | Kiểm mức ảnh hưởng, nguồn, xung đột; chọn theo dõi hoặc can thiệp. UI: Thấy đề xuất hold/recheck và lý do | Session của user/role; không gas. Ngoại lệ: Operator là tác giả: chuyển người khác; báo trùng liên kết hồ sơ | SPECIFIED |
| **U11-E03** (Operator) | S31 / C41, C31. Xác nhận cần ngăn tác hại. `revision.hold_applied.v1` | `POST /revisions/{id}/hold`<br>IN: `revisionId`, `reportId`<br>OUT/ref: `reasonId`, `actionId` | Ghi app hold + desired block + outbox; ngừng tip/promote phần bị ảnh hưởng tại API. UI: SUSPENDED / ONCHAIN_BLOCK_PENDING | Session của user/role; không gas. Ngoại lệ: Không hứa đã chặn contract khi chưa tx; giữ nguyên receipt và tiền quá khứ | SPECIFIED |
| **U11-E04** (W_OPS / W_ADMIN)| S32 / C41, C34. Ví có quyền thực thi block. `revision.chain_blocked.v1` | `setRevisionBlocked`<br>`POST /actions/{id}/attempts`<br>IN: `revisionId`, `actionId`<br>OUT/ref: `attemptId` | Đúng quyền theo manifest; worker ghi trạng thái block quan sát/finalized. UI: BLOCKED khi đã xác minh; hold web vẫn giữ | W_OPS hoặc W_ADMIN theo quyền contract; ví gửi trả gas. Ngoại lệ: Pending quá lâu hiện cảnh báo; tip hợp lệ trong trễ vẫn ghi | SPECIFIED |
| **U11-E05** (Operator) | S12 / C19, C20. Có dữ liệu mới cần xác minh. `review.recheck_opened.v1` | `POST /review-cases`<br>IN: `revisionId`, `reviewCaseId`<br>OUT/ref: `reviewCaseId` | Input case cũ là previousCaseId, output case mới; cùng bản hoặc bản sửa mới. UI: Đi U04-U06; độc lập ngân sách/công cũ | Session của user/role; không gas. Ngoại lệ: Không sửa decision kết thúc cũ; chưa đủ người/ngân sách giữ chờ | SPECIFIED |
| **U11-E06** (Operator + Admin)| S31 / C41, C06, C31. Có decision mới và người xử lý. `revision.resolution_applied.v1` | `POST /decisions/{id}/resolution`<br>unblock action<br>IN: `decisionId`<br>OUT/ref: `actionId`, `attemptId` | Chỉ mở lại khi approval còn hạn, không hold khác, gate onchain sẵn sàng. UI: RESTORE_PENDING rồi sẵn sàng; hoặc tiếp tục thu hồi | Session xử lý nội dung; nếu mở chain, W_ADMIN ký và trả gas. Ngoại lệ: Không phục hồi theo event ACTIVE cũ; hết hạn tự EXPIRED | SPECIFIED |

---

## Luồng U12: Tra Cứu, Đối Soát và Tác Vụ Hệ Thống (F07-F10; D09-D11; Mục 21)

| Mã Sự Kiện & Actor | Kích hoạt / UI | API / ID Đề xuất | Xử lý và Phản hồi | Chữ ký / Nhánh Lỗi & Ngoại lệ | Trạng thái Chặng 00 |
|---|---|---|---|---|---|
| **U12-E01** (Authorized Staff)| S33 / C45. Nhập UUID, displayCode hoặc chain key. `UI_QUERY` | `GET /admin/entity-lookup?type=&id=`<br>IN: `deploymentId`<br>OUT/ref: Giữ hồ sơ hiện tại | Mã dễ đọc resolve UUID; key chain lookup kèm deployment; chỉ trả liên kết được phép. UI: Post -> revision -> case -> task -> payable | Session của user/role; không gas. Ngoại lệ: Không có global public lookup; không trả PII; wrong environment không match | SPECIFIED |
| **U12-E02** (Worker / Ops) | S32 / C44, C34. Chọn action chưa rõ hoặc quét lại. `action.reconciled.v1` | `POST /actions/{id}/reconcile`<br>IN: `actionId`, `attemptId`<br>OUT/ref: Giữ hồ sơ hiện tại | Đọc chain/current business key; phân biệt rejected, reverted, replaced, observed, finalized. UI: Cập nhật projection | Không ký ví; không gas. Ngoại lệ: RPC lag giữ unknown/pending; không tự phát lại giao dịch; Safe outer success chưa đủ | SPECIFIED |
| **U12-E03** (Worker) | S32 / C44, C47. Server clock / scheduled worker. `resource.expiry_projected.v1` | `INTERNAL expiry projection`<br>`GET resource`<br>IN: `revisionId`, `membershipId`<br>OUT/ref: Giữ hồ sơ | Mỗi loại tính hạn riêng từ nguồn; verification expired tắt tip, VIP expired chặn quyền, role expired chặn việc. UI: Hiện hết hạn | Không ký ví; không gas. Ngoại lệ: Không mất lịch sử NFT/SBT chỉ vì bài cũ; client clock không là nguồn quyền | SPECIFIED |
| **U12-E04** (Treasury / Worker)| S28 / C42, C44. Có vốn hoặc tiền vào cần đối chiếu. `treasury.funding_reconciled.v1` | `POST /fundings`<br>`GET /admin/ledger`<br>IN: `deploymentId`<br>OUT/ref: `fundingId`, `actionId` | Phân loại OWNER_FUNDING / DONATION / VIP...; fundingId cho vốn riêng. Ví nguồn ký và đối soát. UI: Quỹ theo asset, nguồn, reserve | Đối soát không ký; nếu chuyển vốn, ví cấp vốn ký và trả gas. Ngoại lệ: Không coi mọi transfer là donate; không cộng tiền 2 lần | SPECIFIED |
| **U12-E05** (Treasury / Author)| S28 / C42, C43, C34. Người có quyền duyệt nghĩa vụ hoàn. `refund.authorized.v1` | `POST /refunds`<br>`POST /refunds/{id}/action`<br>IN: `donationId`, `paymentId`<br>OUT/ref: `refundId`, `actionId` | Một nguồn mỗi hồ sơ: sourceReceiptKey cho donate hoặc paymentId VIP. Xác định phần hoàn, giữ lịch sử gốc. UI: Refund có trạng thái riêng | Ví bên thực sự hoàn tiền ký và trả gas; quỹ và tác giả ký riêng phần mình. Ngoại lệ: Quỹ không tự kéo lại 80% từ tác giả | SPECIFIED |
| **U12-E06** (Admin) | S32 / C49, C41, C34. Admin thay cấu hình đã xác nhận. `admin.change_applied.v1` | `POST /admin/actions`<br>role/pause action<br>IN: `roleAssignmentId`<br>OUT/ref: `actionId`, `attemptId` | Phân biệt role ứng dụng qua session và quyền onchain bằng ví; unpause/admin role do W_ADMIN. UI: Hiện pending/observed/finalized | Role ứng dụng dùng session; quản trị contract dùng W_ADMIN và gas. Ngoại lệ: Không cho Operator nội dung tự chi quỹ | SPECIFIED |
